// npm (node package manager) like pip(in python)
// package : jo mdules kisi aur ne likhi hai bt hm use kr rhe hai
// npm init ne iss folder ko node package ke tarah initialise kr diya
// jo bhi package hm "npm install package_name" se krenge, wo package.json file ke dependencies mai chala jayega
// version : major.minor.patch(bug fix number)
// npm install package_name --save-dev   :  only used to deploy in our production(that project only), so only we want it as devDependencies
// npm install package_name --global   :   we want to install it globally, so can be used for any project.
// nodemon is used to reflect the changes in browser as soon as we do any changes in file 
// npm view nodemon version
// ^1.3.5 : ^ means exact version
// ~1.3.5 : ~ means install latest version considering patch
// >1.3.5 : version greater/newer than this

// Postman is used to get response of a link

console.log("This is basics of npm");
